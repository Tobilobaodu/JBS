# Core
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
pydantic-settings==2.15.0

# Database
sqlalchemy==2.0.54
alembic==1.20.0
asyncpg==0.31.0
psycopg2-binary==2.9.13

# Auth
bcrypt==4.2.1
pyjwt[crypto]==2.14.0

# Storage
boto3==1.43.97

# Queue / Workers
celery[redis]==5.6.3
redis==5.2.1

# LLM (Phase 3 — tailored CV generation)
openai==2.53.0

# File validation
python-magic==0.4.27

# Document export (Sprint 5 — docx generation; PDF conversion is via the
# separate Gotenberg service, called over HTTP, no Python library needed)
docxtpl==0.20.2

# Malware scanning (clamd client)
clamd==1.0.2

# Observability
structlog==24.4.0
prometheus-client==0.26.0
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
factory-boy==3.3.3

# HTTP client (used by tests' TestClient, and at runtime by worker_export
# to call the Gotenberg PDF-conversion service)
httpx==0.28.1

# Email validation (required by Pydantic EmailStr)
email-validator==2.3.0

# Utilities
python-multipart==0.0.32
python-dotenv==1.2.3
